/* Compatibilidade de bindings globais para a camada CMMS V2.
 * A V2 substitui funções do app legado e também cria novas telas. Como o
 * script principal roda em strict mode, declaramos explicitamente aqui todos
 * os bindings globais que ele sobrescreve/cria.
 */
var setMode;
var render;
var renderRegisterFlow;
var doSubmit;
var submitSonda;
var submitFalhaGeral;
var askDel;
var getFilteredData;
var renderHistoryView;
var loadHistory;
var doOpDone;
var askOpDone;
var askDelOp;
var renderOrdersView;
var renderWorkOrdersView;
var renderDashboardView;
var openManual;
